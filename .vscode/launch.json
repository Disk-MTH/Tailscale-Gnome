{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test in nested GNOME Shell",
            "type": "node-terminal",
            "request": "launch",
            "command": "dbus-run-session -- gnome-shell --nested --wayland",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Install"
        }
    ]
}
